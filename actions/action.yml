name: "Read Secrets"
description: "Reads the Secret keys from github secrets"
author: "Kahlil Herring"

inputs:
  NAME:  # id of input
    description: 'Name of user'
    required: true
  AGE:  # id of input
    description: 'Age of user'
    required: true
  YEAR:
    description: 'Year unitl users age'
    required: true
outputs:
  time: # id of output
    description: 'The time we greeted you'
runs:
  using: 'node12'
  main: '../activity2.js'
  run: "npm install"

 